=========================================================================================
                  YOUR FIRST PRISMA MIGRATION & QUERY
=========================================================================================

=========================================================================================
# 0. আমরা এই অংশে কী শিখবো?
=========================================================================================
এই ধাপে আমরা শিখবো:

✔ প্রথম Prisma Migration কীভাবে run করতে হয়  
✔ Migration আসলে কী করে  
✔ Database এ table create হওয়া  
✔ Prisma Client দিয়ে প্রথম Query  
✔ Type-safe Query কাকে বলে  

👉 এটা Prisma শেখার সবচেয়ে IMPORTANT STEP

=========================================================================================
# 1. Migration কী? (Concept Clear করো)
=========================================================================================
Migration মানে হলো —

👉 schema.prisma ফাইলের পরিবর্তন  
👉 Database এর সাথে sync করা  

সহজ ভাষায়:
- schema.prisma = Source of Truth
- Migration = DB version history

Migration করলে:
✔ Table create / update হয়  
✔ Column add / remove হয়  
✔ Constraint apply হয়  

=========================================================================================
# 2. First Prisma Model (Recap)
=========================================================================================
ধরা যাক schema.prisma এ এই model আছে:

-----------------------------------------------------------------------------------------
model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
}
-----------------------------------------------------------------------------------------

এখনো পর্যন্ত:
❌ Database এ table নেই  
✔ শুধু schema define করা আছে  

=========================================================================================
# 3. First Migration Run
=========================================================================================
এই command চালাও:

-----------------------------------------------------------------------------------------
npx prisma migrate dev --name init
-----------------------------------------------------------------------------------------

এখানে:
- migrate dev → development migration
- --name init → migration এর নাম

=========================================================================================
# 4. Migration Run করলে কী হয়?
=========================================================================================
এই ৪টা কাজ একসাথে হয়:

✔ prisma/migrations/ folder তৈরি হয়  
✔ SQL migration file generate হয়  
✔ Database এ table create হয়  
✔ Prisma Client auto-generate হয়  

Folder structure:
-----------------------------------------------------------------------------------------
prisma
└── migrations
    └── 2025xxxx_init
        └── migration.sql
-----------------------------------------------------------------------------------------

👉 এই SQL file Prisma নিজে handle করে  
❌ manually edit করার দরকার নাই

=========================================================================================
# 5. Migration Verify (DB Check)
=========================================================================================
PostgreSQL এ দেখবে:

✔ users table create হয়েছে  
✔ id → primary key  
✔ email → unique constraint  
✔ createdAt → default now()  

👉 মানে schema ↔ database sync ✅

=========================================================================================
# 6. Prisma Client কী?
=========================================================================================
Prisma Client হলো:

👉 Auto-generated TypeScript library  
👉 schema.prisma দেখে তৈরি হয়  

এটা দিয়ে আমরা:
- create
- read
- update
- delete  
সব query করি

Import example:
-----------------------------------------------------------------------------------------
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();
-----------------------------------------------------------------------------------------

=========================================================================================
# 7. Your First Prisma Query (CREATE)
=========================================================================================
চলো প্রথম user create করি:

-----------------------------------------------------------------------------------------
const user = await prisma.user.create({
  data: {
    name: "Ridoan",
    email: "ridoan@gmail.com",
    password: "hashed_password",
  },
});

console.log(user);
-----------------------------------------------------------------------------------------

কি হলো?
✔ INSERT query run হলো  
✔ User DB তে save হলো  
✔ Created object return হলো  

=========================================================================================
# 8. First READ Query (findMany)
=========================================================================================
সব user আনতে:

-----------------------------------------------------------------------------------------
const users = await prisma.user.findMany();

console.log(users);
-----------------------------------------------------------------------------------------

ব্যাখ্যা:
- prisma.user → model নাম
- findMany → SELECT * FROM users

👉 SQL লিখা লাগেনি ❌

=========================================================================================
# 9. findUnique Query
=========================================================================================
Unique field দিয়ে data আনো:

-----------------------------------------------------------------------------------------
const user = await prisma.user.findUnique({
  where: {
    email: "ridoan@gmail.com",
  },
});
-----------------------------------------------------------------------------------------

নিয়ম:
✔ where এ শুধু unique field দেওয়া যায়  

=========================================================================================
# 10. Type Safety Magic (সবচেয়ে বড় ফায়দা)
=========================================================================================
ভুল করলে কী হবে?

-----------------------------------------------------------------------------------------
await prisma.user.create({
  data: {
    name: "Test",
    username: "abc", // ❌ এই field নাই
  },
});
-----------------------------------------------------------------------------------------

👉 TypeScript error দিবে  
👉 Code run হওয়ার আগেই bug ধরা পড়বে  

এইটাই Prisma এর SUPER POWER 💪

=========================================================================================
# 11. Prisma Query ≈ SQL (Comparison)
=========================================================================================
Prisma:
-----------------------------------------------------------------------------------------
prisma.user.findMany();
-----------------------------------------------------------------------------------------

SQL:
-----------------------------------------------------------------------------------------
SELECT * FROM users;
-----------------------------------------------------------------------------------------

👉 Result same  
👉 Prisma clean + safe  

=========================================================================================
# 12. Common Beginner Mistakes
=========================================================================================
❌ Migration না চালিয়ে query করা  
❌ schema change করে migrate না করা  
❌ PrismaClient বারবার create করা  
❌ async/await ভুলে যাওয়া  

=========================================================================================
# 13. Best Practices (Early Stage)
=========================================================================================
✔ schema change → migrate dev  
✔ prisma client আলাদা ফাইলে রাখো  
✔ dev stage এ migrate dev use করো  
✔ production এ migrate deploy use হয়  

=========================================================================================
# 14. Summary (Final Takeaway)
=========================================================================================
- Migration = schema → database sync
- First migrate dev খুব গুরুত্বপূর্ণ
- Prisma Client auto-generate হয়
- Query লেখা সহজ + safe
- SQL জানলেও Prisma speed বাড়ায়

=========================================================================================
                                   THE END
=========================================================================================
