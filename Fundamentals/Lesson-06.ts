=========================================================================================
                     CREATING DATA WITH PRISMA (A–Z GUIDE)
=========================================================================================

=========================================================================================
# 0. আমরা এই অংশে কী শিখবো?
=========================================================================================
এই অধ্যায়ে আমরা শিখবো:

✔ Prisma দিয়ে data create করা  
✔ create() method  
✔ Single record insert  
✔ Multiple record insert  
✔ Relation সহ data create  
✔ Enum সহ data create  
✔ Common mistakes & best practices  

👉 Prisma CRUD এর প্রথম এবং সবচেয়ে গুরুত্বপূর্ণ অংশ

=========================================================================================
# 1. Prisma এ Data Create মানে কী?
=========================================================================================
Data Create মানে হলো —

👉 Database এ নতুন record insert করা  
👉 SQL এর INSERT query এর equivalent  

Prisma এ:
-----------------------------------------------------------------------------------------
create()
createMany()
-----------------------------------------------------------------------------------------
এই দুইটা method ব্যবহার হয়

=========================================================================================
# 2. Basic create() Syntax
=========================================================================================
-----------------------------------------------------------------------------------------
await prisma.modelName.create({
  data: {
    field1: value,
    field2: value,
  },
});
-----------------------------------------------------------------------------------------

Rules:
✔ data object mandatory  
✔ সব required field দিতে হবে  
✔ Type-safe validation হয়  

=========================================================================================
# 3. Simple Create Example (Single Record)
=========================================================================================
User create করি:

-----------------------------------------------------------------------------------------
await prisma.user.create({
  data: {
    name: "Ridoan",
    email: "ridoan@gmail.com",
    password: "hashed_password",
  },
});
-----------------------------------------------------------------------------------------

কি হলো?
✔ DB তে নতুন user insert  
✔ Unique constraint check  
✔ Created object return  

=========================================================================================
# 4. Create With Return Data
=========================================================================================
Create করার পর data পাওয়া যায়:

-----------------------------------------------------------------------------------------
const user = await prisma.user.create({
  data: {
    name: "Sumi",
    email: "sumi@gmail.com",
    password: "hashed_password",
  },
});

console.log(user);
-----------------------------------------------------------------------------------------

👉 Created row return করে

=========================================================================================
# 5. createMany() (Multiple Records Insert)
=========================================================================================
একসাথে অনেক data insert:

-----------------------------------------------------------------------------------------
await prisma.user.createMany({
  data: [
    {
      name: "Rafi",
      email: "rafi@gmail.com",
      password: "pass1",
    },
    {
      name: "Karim",
      email: "karim@gmail.com",
      password: "pass2",
    },
  ],
});
-----------------------------------------------------------------------------------------

Important:
❌ createMany() created data return করে না  
✔ Fast bulk insert  

=========================================================================================
# 6. Create With Enum Field
=========================================================================================
Enum সহ create:

-----------------------------------------------------------------------------------------
await prisma.user.create({
  data: {
    name: "Admin User",
    email: "admin@gmail.com",
    password: "admin_pass",
    role: "ADMIN",
  },
});
-----------------------------------------------------------------------------------------

Rule:
✔ Enum value exact match হতে হবে  
❌ Random string allowed না  

=========================================================================================
# 7. Create With Optional Field
=========================================================================================
Optional field skip করা যায়:

-----------------------------------------------------------------------------------------
await prisma.user.create({
  data: {
    name: "Optional Test",
    email: "opt@gmail.com",
    password: "1234",
    // phone দেওয়া হয়নি
  },
});
-----------------------------------------------------------------------------------------

👉 phone NULL থাকবে

=========================================================================================
# 8. Create With One-to-One Relation
=========================================================================================
User + Profile একসাথে create:

-----------------------------------------------------------------------------------------
await prisma.user.create({
  data: {
    email: "profile@gmail.com",
    profile: {
      create: {
        bio: "Backend Developer",
      },
    },
  },
});
-----------------------------------------------------------------------------------------

👉 Nested write (relation create)

=========================================================================================
# 9. Create With One-to-Many Relation
=========================================================================================
User + Posts:

-----------------------------------------------------------------------------------------
await prisma.user.create({
  data: {
    name: "Post User",
    posts: {
      create: [
        { title: "First Post" },
        { title: "Second Post" },
      ],
    },
  },
});
-----------------------------------------------------------------------------------------

👉 Parent + child একসাথে insert

=========================================================================================
# 10. Create With Existing Relation (connect)
=========================================================================================
আগে থেকে থাকা user এর সাথে post যুক্ত করা:

-----------------------------------------------------------------------------------------
await prisma.post.create({
  data: {
    title: "Connected Post",
    user: {
      connect: {
        id: 1,
      },
    },
  },
});
-----------------------------------------------------------------------------------------

👉 Foreign key manually দিতে হয়নি

=========================================================================================
# 11. Create With Many-to-Many Relation
=========================================================================================
Student + Course (implicit):

-----------------------------------------------------------------------------------------
await prisma.student.create({
  data: {
    name: "Ridoan",
    courses: {
      connect: [
        { id: 1 },
        { id: 2 },
      ],
    },
  },
});
-----------------------------------------------------------------------------------------

=========================================================================================
# 12. Create With Explicit Many-to-Many
=========================================================================================
Enrollment create:

-----------------------------------------------------------------------------------------
await prisma.enrollment.create({
  data: {
    studentId: 1,
    courseId: 2,
    status: "ACTIVE",
  },
});
-----------------------------------------------------------------------------------------

=========================================================================================
# 13. Common Create Errors
=========================================================================================
❌ Required field miss করা  
❌ Enum value ভুল দেওয়া  
❌ Relation connect ভুল id  
❌ Unique constraint violate  
❌ Migration না চালানো  

=========================================================================================
# 14. Best Practices
=========================================================================================
✔ Always validate input  
✔ Enum ব্যবহার করো status/role এর জন্য  
✔ Relation create/connect বুঝে ব্যবহার করো  
✔ createMany শুধুমাত্র bulk insert এ  
✔ Error handling করো (try/catch)

=========================================================================================
# 15. Summary (Final Takeaway)
=========================================================================================
- create() = single record insert
- createMany() = bulk insert
- Nested create দিয়ে relation handle করা যায়
- Enum data consistency নিশ্চিত করে
- Prisma create fully type-safe
- Production backend এর foundation

=========================================================================================
                                   THE END
=========================================================================================
