=========================================================================================
              READING DATA USING SELECT & INCLUDE (PRISMA)
=========================================================================================

=========================================================================================
# 0. আমরা এই অংশে কী শিখবো?
=========================================================================================
এই অধ্যায়ে আমরা শিখবো:

✔ Prisma দিয়ে data read করা  
✔ select কী  
✔ include কী  
✔ select vs include পার্থক্য  
✔ Relation data fetch করা  
✔ Nested select/include  
✔ Real-world examples  
✔ Common mistakes & best practices  

👉 Efficient data fetching এর CORE concept

=========================================================================================
# 1. Prisma এ Data Read মানে কী?
=========================================================================================
Data Read মানে হলো —

👉 Database থেকে data আনা  
👉 SQL এর SELECT query এর equivalent  

Prisma এ:
-----------------------------------------------------------------------------------------
findMany()
findUnique()
findFirst()
-----------------------------------------------------------------------------------------
এই method গুলো ব্যবহার হয়

=========================================================================================
# 2. Default Behavior (No select / include)
=========================================================================================
-----------------------------------------------------------------------------------------
const users = await prisma.user.findMany();
-----------------------------------------------------------------------------------------

কি হয়?
✔ User এর সব column আসে  
❌ Relation data আসে না  

👉 Default = only model fields

=========================================================================================
# 3. select কী?
=========================================================================================
select ব্যবহার হয় —

👉 নির্দিষ্ট column আনতে  
👉 Unnecessary data বাদ দিতে  
👉 Performance improve করতে  

Rule:
✔ select দিলে include দেওয়া যাবে না (same level এ)

=========================================================================================
# 4. Basic select Example
=========================================================================================
-----------------------------------------------------------------------------------------
const users = await prisma.user.findMany({
  select: {
    id: true,
    name: true,
    email: true,
  },
});
-----------------------------------------------------------------------------------------

Result:
👉 শুধু id, name, email আসবে

=========================================================================================
# 5. select With Relation
=========================================================================================
Relation এর নির্দিষ্ট field আনতে:

-----------------------------------------------------------------------------------------
const users = await prisma.user.findMany({
  select: {
    name: true,
    bookings: {
      select: {
        startDate: true,
        endDate: true,
      },
    },
  },
});
-----------------------------------------------------------------------------------------

👉 Nested select allowed

=========================================================================================
# 6. include কী?
=========================================================================================
include ব্যবহার হয় —

👉 Relation এর full data আনতে  
👉 JOIN এর মতো কাজ করে  

Rule:
✔ include শুধু relation এর জন্য  
❌ Scalar field এর জন্য না

=========================================================================================
# 7. Basic include Example
=========================================================================================
-----------------------------------------------------------------------------------------
const users = await prisma.user.findMany({
  include: {
    bookings: true,
  },
});
-----------------------------------------------------------------------------------------

কি হবে?
✔ User এর সব field  
✔ সাথে bookings relation এর সব field  

=========================================================================================
# 8. include With Nested include
=========================================================================================
Multiple relation include:

-----------------------------------------------------------------------------------------
const bookings = await prisma.booking.findMany({
  include: {
    user: true,
    vehicle: true,
  },
});
-----------------------------------------------------------------------------------------

👉 booking + user + vehicle একসাথে

=========================================================================================
# 9. select vs include (Core Difference)
=========================================================================================
-----------------------------------------------------------------------------------------
select  → Field control (what to fetch)
include → Relation data (join)
-----------------------------------------------------------------------------------------

Example comparison:

select:
-----------------------------------------------------------------------------------------
select: {
  name: true,
}
-----------------------------------------------------------------------------------------

include:
-----------------------------------------------------------------------------------------
include: {
  bookings: true,
}
-----------------------------------------------------------------------------------------

=========================================================================================
# 10. select + include একসাথে ব্যবহার?
=========================================================================================
❌ একই level এ করা যাবে না:

-----------------------------------------------------------------------------------------
findMany({
  select: { name: true },
  include: { bookings: true },
});
-----------------------------------------------------------------------------------------

✔ Nested level এ করা যায়:

-----------------------------------------------------------------------------------------
select: {
  name: true,
  bookings: {
    include: {
      vehicle: true,
    },
  },
}
-----------------------------------------------------------------------------------------

=========================================================================================
# 11. include With Field Control
=========================================================================================
include এর ভিতরে select ব্যবহার:

-----------------------------------------------------------------------------------------
const users = await prisma.user.findMany({
  include: {
    bookings: {
      select: {
        startDate: true,
        status: true,
      },
    },
  },
});
-----------------------------------------------------------------------------------------

👉 Relation field control সম্ভব

=========================================================================================
# 12. Real-World Example (Vehicle Rental)
=========================================================================================
সব booking সহ user data:

-----------------------------------------------------------------------------------------
const users = await prisma.user.findMany({
  select: {
    id: true,
    name: true,
    bookings: {
      include: {
        vehicle: {
          select: {
            model: true,
          },
        },
      },
    },
  },
});
-----------------------------------------------------------------------------------------

👉 Optimized & clean response

=========================================================================================
# 13. Performance Tips
=========================================================================================
✔ Always use select when possible  
✔ Unnecessary include এড়িয়ে চলো  
✔ Deep nesting avoid করো  
✔ Large data → pagination use করো  

=========================================================================================
# 14. Common Mistakes
=========================================================================================
❌ select + include same level  
❌ include দিয়ে scalar field আনতে চাওয়া  
❌ Overfetching data  
❌ Relation name ভুল লেখা  

=========================================================================================
# 15. Summary (Final Takeaway)
=========================================================================================
- findMany / findUnique দিয়ে data read
- select → column control
- include → relation fetch
- Nested select/include powerful
- Correct usage = better performance
- Prisma data fetching clean & safe
=========================================================================================
                                   THE END
=========================================================================================
